---
name: "technical-archive-scan"
title: "技术档案扫描风图片处理"
description: "将用户图片统一处理成低饱和、荧光绿扫描框、数据标签、局部特写、监控识别式编号线框与错落带图信息框的技术档案扫描风格。"
version: 1
namespace: "user-generated"
extended:
  cover: "https://raw.githubusercontent.com/shift-ailab/shift-skills/main/covers/technical-archive-scan.jpg"
  keywords: "技术档案,扫描风,荧光绿标注,监控识别,编号线框,错落信息框,图片处理,风格化"
  template: "帮我把[这组图片]统一处理成技术档案扫描风格：画面加荧光绿扫描框、数据标签卡片、侧边竖排文字和底部信息条，整体低饱和冷灰调，只保留我提供的主体内容，不加入原参考图里的品牌或场景信息。"
  tool_policy:
    allowed_tools:
      - "skill_file"
      - "write_skill"
      - "ask_human"
    enforcement: "enforce"
    mode: "restrict"
    version: 1
---

## 目标
把用户提供的任意图片统一处理成"技术扫描档案风"视觉格式：低饱和画面 + 荧光绿高亮扫描框 + 数据标签 + 主图与局部特写组合 + 主体身体部位/关键区域编号线框 + 错落带图/文字信息框 + 竖排/横排文字信息层。

## 核心视觉规则（仅格式，不含原参考图的具体画面信息）
1. **扫描框层**：用荧光绿矩形框圈选画面中的关键对象（人物、单品、局部），框线纤细，四角可略尖或带轻微科技边角。
2. **主体部位编号线框**：在主体全身/全景基础上，用额外的荧光绿矩形线框标记身体部位或关键区域（如头部、躯干、手臂、配饰、单品局部等），线框可带编号标签（如 01 / 02 / A1 / SECTOR-1 等）或简短标识，形成监控识别与数据标注感。编号标签字体极简、字号小、与线框同色或反色，不遮挡主体关键细节。
3. **错落带图信息框**：从扫描框或编号标签引出细线，连接 1–2 个包含局部特写图的信息框（如被框选部位的放大、面部、配饰、单品局部等），其余信息框可仅含文字数据（ID、TYPE、NOTES 等占位）。所有信息框均不排列在画面四周边缘，而是错落分布于画面内部、靠近被指向的线框位置，可轻微重叠、大小不一、朝向略有变化，以增加空间层级感与监控标注的真实感。信息框使用荧光绿描边或半透明荧光绿底，文字/编号极小，不整齐对齐。
4. **连接线 + 主信息卡片**：从扫描框或编号标签引出细线，连接画面一侧或下方的主信息卡片；卡片内包含 ID、PRODUCT、UNITS 等字段占位，使用荧光绿底 + 深色字或深底 + 荧光绿字。
5. **主图 + 特写组合**：保留主体全身/全景，同时在画面中安排一处小尺寸局部特写（如上半身、配饰、面部），与主体形成档案对照。
6. **竖排侧边文字**：在画面左侧或右侧边缘加入竖排小字标签（如 OWN / FASHION / TIME / STYLE 等抽象分类词），字间距宽松，颜色为荧光绿或浅灰。
7. **底部信息条**：在画面底部加入横排细线信息条，包含系列名占位、年份、编号、COLLECTION 等字段，字体极简无衬线。
8. **顶部细节**：左上角可加入日期/编号占位，右上角可加入电量/信号/进度等 UI 微标，营造数字设备界面感。
9. **色彩基调**：背景保持低饱和、偏冷灰或灰绿；荧光绿（近似 #00FF00 或 #39FF14）作为唯一强调色，面积小而集中。
10. **质感**：整体略带监控画面、扫描档案、低像素或轻微噪点，但主体清晰可辨。

## 执行流程
1. 确认用户提供了一张或多张待处理图片。
2. 读取图片主体内容，确定扫描框应圈选的关键对象，以及主体身体部位/关键区域的位置（如头部、躯干、手臂、配饰、单品局部等）。
3. 按上述格式规则生成编辑/重绘提示词，明确：
   - 保留原图主体身份与内容；
   - 叠加扫描框、数据标签、连接线、错落带图/文字信息框、竖排文字、底部信息条；
   - 增加主体身体部位/关键区域的矩形编号线框，营造监控识别与数据标注感；
   - 信息框错落分布于画面内部、靠近所指线框，不整齐排列，可轻微重叠；
   - 色调压低为冷灰/灰绿，荧光绿作强调色；
   - 不出现参考图中的具体品牌名、人物名、场景名。
4. 使用 `edit_media` 或 `generate_media` 进行风格化处理（单张编辑用 `edit_image`，多张系列输出时用 `generate_image` 并共享同一格式锚点）。
5. 若用户要求"统一处理"多张图，先确认是否要保持完全一致的版式（框的位置、卡片位置、侧边文字），还是根据各图主体自适应。默认在保持同一套视觉系统的前提下，根据每张图的主体位置做自适应扫描框、部位编号线框与错落信息框。
6. 输出完成后，向用户展示结果并询问是否需要微调版式、部位线框位置、信息框分布或信息卡片文案。

## 约束
- **禁止**在生成结果中写入原参考图里的具体品牌、人物、地点、场景信息。
- 信息卡片与编号标签中的文字仅使用占位符或用户明确提供的新文案；不要编造真实产品名或真实编号。
- 荧光绿为唯一强对比色，避免引入其他高饱和色彩，除非用户特别要求。
- 文字信息层与编号标签的字号要小、排版要疏，不能遮挡主体关键细节。
- 带图信息框数量控制在 1–2 个，其余可为纯文字数据框；信息框不贴边、不整齐排列，须在画面内部错落分布以形成层级感。
